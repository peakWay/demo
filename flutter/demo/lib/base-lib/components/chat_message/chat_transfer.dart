
import 'package:flutter/material.dart';

class ChatTransfer extends StatelessWidget {
  // ChatTransfer({
    
  // })

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}